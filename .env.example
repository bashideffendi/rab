# Postgres connection string (Railway / Neon / Supabase / lokal)
# Format: postgres://user:password@host:port/database
DATABASE_URL="postgres://user:password@localhost:5432/rabin"

# Public app URL (untuk meta tags, OG, dll)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
